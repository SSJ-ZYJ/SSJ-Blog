# Code Wiki

## 项目概览

这是一个基于Astro框架构建的静态博客网站，使用Svelte和Tailwind CSS等技术实现现代化的前端界面。

- **响应式设计**：适配各种设备屏幕尺寸
- **多语言支持**：支持中文、英文等多种语言
- **主题切换**：内置明暗模式切换功能
- **代码高亮**：使用Expressive Code实现美观的代码块展示
- **评论系统**：集成Giscus评论系统
- **内容管理**：使用Astro Content Collections管理博客文章

## 目录结构

项目采用模块化的目录结构，清晰地分离了不同功能模块：

```
├── public/             # 静态资源目录
│   ├── assets/         # 图片等资源
│   ├── favicon/        # 网站图标
│   └── wallpaper/      # 背景图片
├── scripts/            # 辅助脚本
├── src/                # 源代码目录
│   ├── assets/         # 项目资源
│   ├── components/     # 组件
│   ├── constants/      # 常量定义
│   ├── content/        # 内容文件
│   ├── i18n/           # 国际化
│   ├── layouts/        # 布局组件
│   ├── pages/          # 页面
│   ├── plugins/        # 插件
│   ├── styles/         # 样式文件
│   ├── types/          # 类型定义
│   ├── utils/          # 工具函数
│   ├── config.ts       # 项目配置
│   └── content.config.ts # 内容配置
├── astro.config.mjs    # Astro配置
├── package.json        # 项目依赖
└── tsconfig.json       # TypeScript配置
```

## 核心功能模块

### 1. 页面系统

页面系统基于Astro的文件系统路由，主要包含以下页面：

- **首页**：展示博客文章列表，支持分页
- **文章页**：展示单篇博客文章内容
- **归档页**：按时间顺序展示所有文章
- **关于页**：展示个人信息

### 2. 内容管理

使用Astro Content Collections管理博客文章，支持Markdown格式：

- **文章结构**：包含标题、发布日期、标签、分类等元数据
- **内容组织**：按目录结构组织文章，支持嵌套分类
- **草稿功能**：支持标记为草稿的文章在生产环境不显示

### 3. 组件系统

项目包含多种类型的组件：

- **控制组件**：分页、回到顶部等
- **小部件**：侧边栏、标签云、分类列表等
- **布局组件**：主布局、网格布局等
- **功能组件**：搜索、主题切换等

### 4. 国际化

支持多语言切换，包含：

- **语言文件**：中文、英文等语言的翻译
- **国际化键值**：统一管理翻译键
- **语言切换**：根据用户设置或浏览器语言自动切换

### 5. 主题系统

支持明暗模式切换，包含：

- **主题配置**：可配置主题色、背景等
- **模式切换**：手动切换或自动根据系统设置
- **主题样式**：根据模式动态加载不同样式

## 关键类与函数

### 内容处理

1. **getSortedPosts** - 获取排序后的博客文章列表
   - **参数**：无
   - **返回值**：排序后的文章数组，包含前一篇和后一篇文章的链接
   - **用途**：用于首页和归档页的文章列表展示

2. **getTagList** - 获取标签列表及使用次数
   - **参数**：无
   - **返回值**：标签对象数组，包含标签名和使用次数
   - **用途**：用于标签云展示

3. **getCategoryList** - 获取分类列表及文章数量
   - **参数**：无
   - **返回值**：分类对象数组，包含分类名、文章数量和URL
   - **用途**：用于分类导航和展示

### 配置管理

1. **siteConfig** - 网站基本配置
   - **属性**：标题、副标题、语言、主题色等
   - **用途**：控制网站的基本信息和外观

2. **navBarConfig** - 导航栏配置
   - **属性**：导航链接列表
   - **用途**：控制导航栏的显示内容

3. **profileConfig** - 个人资料配置
   - **属性**：头像、姓名、简介、社交媒体链接等
   - **用途**：在个人资料组件中展示个人信息

### 工具函数

1. **getSlugFromId** - 从文章ID生成slug
   - **参数**：文章ID
   - **返回值**：文章slug
   - **用途**：用于生成文章的URL路径

2. **getCategoryUrl** - 生成分类的URL
   - **参数**：分类名称
   - **返回值**：分类的URL路径
   - **用途**：用于分类导航链接

## 技术栈与依赖

| 技术/依赖 | 版本 | 用途 |
|----------|------|------|
| Astro | 6.1.3 | 静态站点生成框架 |
| Svelte | 5.55.1 | 前端组件框架 |
| Tailwind CSS | 3.4.19 | 实用优先的CSS框架 |
| TypeScript | 5.9.3 | 类型安全的JavaScript超集 |
| Expressive Code | 0.41.7 | 代码块高亮和美化 |
| Giscus | - | 基于GitHub Discussions的评论系统 |
| Pagefind | 1.4.0 | 静态站点搜索 |
| Swup | 1.8.0 | 页面过渡动画 |

## 项目配置

### 核心配置文件

1. **astro.config.mjs** - Astro框架配置
   - 集成了Tailwind、Svelte、Sitemap等插件
   - 配置了Markdown处理插件，支持GFM、数学公式等
   - 配置了代码高亮和自定义组件

2. **src/config.ts** - 网站配置
   - 网站基本信息（标题、副标题、语言等）
   - 主题配置（主题色、背景等）
   - 导航栏配置
   - 个人资料配置
   - 评论系统配置

3. **src/content.config.ts** - 内容配置
   - 定义了文章的 schema
   - 配置了内容集合

### 环境变量

项目使用Vite的环境变量系统，主要环境变量包括：

- **NODE_ENV**：运行环境（development/production）
- **PUBLIC_SITE_URL**：网站URL（可选）

## 开发与部署

### 开发流程

1. **安装依赖**：
   ```bash
   pnpm install
   ```

2. **启动开发服务器**：
   ```bash
   pnpm dev
   ```

3. **检查类型**：
   ```bash
   pnpm type-check
   ```

4. **构建项目**：
   ```bash
   pnpm build
   ```

5. **预览构建结果**：
   ```bash
   pnpm preview
   ```

### 部署方式

项目支持多种部署方式：

1. **Vercel**：通过 `vercel.json` 配置，支持自动部署
2. **Netlify**：支持通过Git仓库自动部署
3. **GitHub Pages**：可通过GitHub Actions构建并部署
4. **其他静态站点托管服务**：可将 `dist` 目录部署到任何静态站点托管服务

## 目录与文件说明

### 主要目录

- **src/components/**：包含所有UI组件
- **src/content/posts/**：博客文章内容
- **src/pages/**：页面路由
- **src/utils/**：工具函数
- **src/i18n/**：国际化文件
- **src/styles/**：样式文件

### 关键文件

1. **src/pages/[...page].astro**：首页和分页页面
2. **src/pages/posts/[...slug].astro**：文章详情页
3. **src/components/PostPage.astro**：文章列表组件
4. **src/components/PostCard.astro**：文章卡片组件
5. **src/utils/content-utils.ts**：内容处理工具函数
6. **src/config.ts**：网站配置
7. **src/content.config.ts**：内容配置

## 功能扩展

### 新增功能

1. **添加新页面**：在 `src/pages/` 目录下创建新的Astro文件
2. **添加新组件**：在 `src/components/` 目录下创建新的组件文件
3. **添加新文章**：在 `src/content/posts/` 目录下创建新的Markdown文件
4. **添加新语言**：在 `src/i18n/languages/` 目录下创建新的语言文件

### 自定义配置

1. **修改网站配置**：编辑 `src/config.ts` 文件
2. **修改内容配置**：编辑 `src/content.config.ts` 文件
3. **修改样式**：编辑 `src/styles/` 目录下的样式文件
4. **修改构建配置**：编辑 `astro.config.mjs` 文件

## 常见问题与解决方案

1. **文章不显示**：检查文章的 `draft` 属性是否为 `false`
2. **图片不显示**：确保图片路径正确，静态图片应放在 `public/` 目录下
3. **评论系统不工作**：检查 `giscusConfig` 配置是否正确，确保GitHub仓库已启用Discussions
4. **搜索功能不工作**：确保构建时已运行 `pagefind` 命令

## 性能优化

1. **图片优化**：使用适当大小的图片，考虑使用WebP格式
2. **代码分割**：利用Astro的自动代码分割功能
3. **缓存策略**：合理设置静态资源缓存
4. **减少第三方依赖**：只使用必要的依赖

## 安全注意事项

1. **XSS防护**：使用Astro的安全默认设置，避免直接插入HTML
2. **依赖安全**：定期更新依赖，避免使用有安全漏洞的版本
3. **CSP设置**：考虑设置内容安全策略

## 总结

本项目是一个现代化的静态博客网站，使用Astro、Svelte和Tailwind CSS等技术栈构建。它具有响应式设计、多语言支持、主题切换、代码高亮和评论系统等功能，适合作为个人技术博客或内容展示网站。

项目结构清晰，配置灵活，易于扩展和维护。通过Astro的静态站点生成能力，确保了网站的加载速度和性能。